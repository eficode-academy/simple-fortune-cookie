name: Go
on:
  # Trigger the workflow on push or pull request,
  # but only for the main branch
  push:
#    branches:
#      - development
env:
  docker_username: ${{ secrets.DOCKERHUB_USERNAME }}
  docker_password: ${{ secrets.DOCKERHUB_TOKEN }}
jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: sudo apt-get update  && sudo apt-get install golang-go -y
      - name: Build
        run: cd frontend && go build .
      - name: Test
        run: cd frontend && go test .
  Push_to_docker:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: check out
        uses: actions/checkout@v3
      - name: envtest
      
        run: echo env.docker_username && echo $env.docker_username && echo ${{ env.docker_username}} && echo ${{secrets.docker_username}}
      - name: login
        uses: docker/login-action@v1
        with:
          username: nicobah
          password: dckr_pat_wR0rsVxvl06euxIjWshxdLCipYk
      - name: Build and push Docker back
        uses: docker/build-push-action@ad44023a93711e3deb337508980b4b5e9bcdc5dc
        with:
          context: ./backend
          push: true
          tags: nicobah/devops_backend
      - name: Build and push Docker front
        uses: docker/build-push-action@ad44023a93711e3deb337508980b4b5e9bcdc5dc
        with:
          context: ./frontend
          push: true
          tags: nicobah/devops_frontend
  Test_kubernetes:
    runs-on: self-hosted

    steps:
      - name: check out
        uses: actions/checkout@v3
      - name: setup cluster
        run: chmod +x ./kubernetes/td.sh && bash ./kubernetes/td.sh
      - name: test cluster
        run: chmod +x ./kubernetes/test.sh && bash ./kubernetes/test.sh
