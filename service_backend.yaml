apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: deployment-backend
  name: service-backend
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: deployment-backend
  type: NodePort
status: 
  loadbalancer: {}