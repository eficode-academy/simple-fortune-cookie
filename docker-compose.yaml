version: "3.9"
services:
  frontend:
    build:
      dockerfile: Dockerfile
      context: ./frontend
    
    ports:
    - "8080:9000"
  backend:
    image: awesome/database
    build:
      context: ./backend
    ports:
    - "9000:6379"