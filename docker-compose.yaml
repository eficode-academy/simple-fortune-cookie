version: '3.1'

services:
    frontend:
        build: frontend/
        ports:
            - 8080:8080
        depends_on: 
            - backend
    backend:
        build: backend/
        depends_on: 
            - redisDB
    redisDB:
        image: redis
        restart: always