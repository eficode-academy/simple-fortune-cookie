version: "3.5"
services:
    backend:
        build: ./backend/
        restart: always
        container_name: backend

    frontend:
        build: ./frontend/
        restart: always
        container_name: frontend
        ports:
            - "8080:8080"
        depends_on: 
            - backend