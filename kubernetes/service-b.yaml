# TODO
apiVersion: v1
kind: Service
metadata:
  name: service-b
  labels: 
    app: backend
spec:
  selector:
    app: backend
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 9000
    targetPort: 9000